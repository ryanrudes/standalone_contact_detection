# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Generated media (regeneratable via viz.py / detect.py) — kept out of git; a few demo
# GIFs live in assets/ for the README.
media/
contact_detection.png
contact_graph.png

# OS / editor / local agent state
.DS_Store
.claude/
.cursor/
contact_timeline.png

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Vendored sub-repo (own git history; formalize as a git submodule once it has a remote)
/markovlib/
